cmake_minimum_required(VERSION 3.0.0)
project(myX11 VERSION 0.1.0)
SET(CMAKE_BUILD_TYPE "Debug")

include(CTest)
enable_testing()
add_subdirectory(src bin)
set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
